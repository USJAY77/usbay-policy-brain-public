from __future__ import annotations

# Purpose: reserved boundary for runtime attestation-specific helper expansion.
# Governance scope: attestation freshness and node identity test fixtures.
# Fail-closed expectation: future helpers must validate before returning evidence.
# Sensitive-data handling: future helpers must never log private keys or raw device IDs.

__all__: list[str] = []
