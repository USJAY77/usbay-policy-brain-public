from __future__ import annotations

# Purpose: reserved boundary for WORM evidence helper expansion.
# Governance scope: archive manifests, retention checks, and replica hash assertions.
# Fail-closed expectation: future helpers must reject archive ambiguity.
# Sensitive-data handling: future helpers must operate on hashes and tenant IDs only.

__all__: list[str] = []
