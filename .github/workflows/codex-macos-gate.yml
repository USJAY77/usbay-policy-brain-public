name: codex-macos

on:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  gate:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
actions/checkout@v6
      - run: echo "macOS governance gate active"
