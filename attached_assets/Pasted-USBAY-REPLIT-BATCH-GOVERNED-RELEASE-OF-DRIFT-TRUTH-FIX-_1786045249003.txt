USBAY REPLIT BATCH — GOVERNED RELEASE OF DRIFT-TRUTH FIX TO PRODUCTION

OBJECTIVE

Ship the already validated drift-truth correction through the governed release flow so production reflects the same truthful state already verified locally.

CURRENT VERIFIED LOCAL STATE

STATUS: PASS

Verified:
- SHA chain match: YES
- Policy hash match: YES
- Registry hash match: YES
- Evidence hash match: YES
- Verifier state match: YES
- Sync state match: YES
- Fail-closed preserved: YES
- Execution blocked when not ready: YES
- Tests: 31/31 PASS

Current truthful local badge semantics:
- PILOT · DEGRADED
- VERIFIER NOT ENROLLED
- SYNC SYNCED

Important:
PILOT · DEGRADED is currently truthful because device identity / challenge-renewal / verifier enrollment is not configured in this environment.

Do NOT turn PILOT DEGRADED green.
Do NOT fake verifier enrollment.
Do NOT suppress missing-control state.

AUTHORIZED FILE SET

Only include the already validated drift-truth change set and any strictly required release metadata generated by the existing governed release mechanism.

Expected validated files include:

- gateway/app.py
- tests/test_dashboard_drift_truth.py
- evidence/audit/DRIFT_TRUTH_AUDIT_20260806.json

If additional files appear:
STOP and report them before commit unless they are deterministic release artifacts produced by the existing approved release process.

DO NOT

- add new features
- redesign UI
- change Policy Brain decisions
- change Enforcement Gateway execution authorization semantics
- weaken fail-closed behavior
- rewrite Git history
- delete prior audit evidence
- change unrelated files
- force badge values
- hard-code SYNCED/VERIFIED
- enroll fake device/verifier identities
- expose tokens/secrets
- log credentials
- modify tariff/pricing logic
- modify game behavior

PHASE 1 — PRE-RELEASE BOUNDARY CHECK

Before any commit:

1. Show:
   git status --short
   git branch --show-current
   git rev-parse HEAD
   git rev-parse origin/<tracked-branch>

2. Confirm:
   - only approved files are modified/untracked
   - no unrelated local changes
   - worktree boundary is safe
   - current branch has valid upstream
   - GitHub authentication works
   - no merge/rebase is required

3. Re-run focused drift-truth tests.

Required:
- 31/31 PASS or better
- no regressions

4. Re-run existing governance/fail-closed checks relevant to:
   - runtime commit matching
   - evidence integrity
   - policy/registry integrity
   - execution blocked before READY
   - mismatch restores DRIFT/DEGRADED

If any required check fails:
STOP.
DO NOT COMMIT.
DO NOT PUSH.
DO NOT DEPLOY.

PHASE 2 — COMMIT

If Phase 1 passes:

Create exactly ONE governed commit for this release.

Commit title:

fix(governance): align drift badges with authoritative live state

Commit description/body:

- derive sync status from authoritative repository/deployment/runtime comparison
- distinguish verifier not-enrolled from verifier drift
- preserve truthful pilot degraded state while required controls are unenrolled
- add drift-truth regression coverage
- add non-sensitive audit evidence
- preserve fail-closed execution semantics

Do not include unrelated files.

After commit verify:
- commit SHA
- files committed
- worktree clean

PHASE 3 — PUSH

Push only the current governed branch to its configured origin.

Verify:
- push accepted
- local HEAD == remote branch HEAD
- no unexpected remote divergence

Do not force-push.

PHASE 4 — GOVERNED DEPLOYMENT

Deploy only through the existing approved governed release path.

Requirements:

- inject EXPECTED_GIT_COMMIT from the exact reviewed commit SHA
- production deployment SHA must equal reviewed GitHub SHA
- runtime SHA must equal deployment SHA
- runtime commit stamp must equal reviewed SHA
- startup mismatch must fail closed
- execution_authorized=false until READY
- no manual production edits

If deployment reports an unexpected SHA:
STOP and mark deployment FAIL.

PHASE 5 — LIVE PRODUCTION VALIDATION

After deployment, verify production independently.

Required SHA chain:

GitHub HEAD
==
deployed commit
==
expected_git_commit
==
deployment SHA
==
runtime SHA
==
runtime commit stamp

Required governance truth:

POLICY HASH MATCH: YES
REGISTRY HASH MATCH: YES
EVIDENCE HASH MATCH: YES
SYNC STATE MATCH: YES

Required production badge state:

- SYNC SYNCED
- VERIFIER NOT ENROLLED
- PILOT · DEGRADED

PILOT DEGRADED must remain until actual enterprise enrollment is completed.

The production UI must NOT show:
- SYNC DRIFT when the SHA chain matches
- VERIFIER DRIFT merely because no verifier is enrolled

PHASE 6 — NEGATIVE TRUTH TEST

Prove the fix did not weaken detection.

Using safe non-production/designed test paths only:

1. SHA mismatch -> SYNC DRIFT / fail-closed
2. verifier mismatch after enrollment state exists -> VERIFIER DRIFT
3. verifier absent/not enrolled -> NOT ENROLLED, not DRIFT
4. missing required pilot control -> PILOT DEGRADED
5. STARTING/VERIFYING -> execution_authorized=false
6. invalid evidence -> degraded or blocked according to existing policy

Do not manufacture production failure.

PHASE 7 — RELEASE EVIDENCE

Create/update a non-sensitive governed release evidence artifact containing:

- timestamp
- release commit SHA
- GitHub SHA
- deployment SHA
- runtime SHA
- policy hash
- registry hash
- evidence hash
- badge values before release
- badge values after release
- focused test results
- fail-closed negative test results
- rollback reference
- deployment result
- remaining gaps

No secrets.
No tokens.
No authorization headers.
No customer data.

PHASE 8 — ROLLBACK READINESS

Confirm a verified previous production release remains available as rollback target.

Return:
- rollback SHA/reference
- rollback path verified YES/NO

Do not execute rollback unless deployment validation fails.

PHASE 9 — FINAL DECISION

ENTERPRISE PILOT READY may be YES only if:

- governed release succeeded
- production SHA chain matches
- policy/registry/evidence hashes match
- SYNC SYNCED is visible
- VERIFIER NOT ENROLLED is visible
- PILOT DEGRADED remains truthful
- fail-closed remains intact
- rollback is ready
- no secret leakage
- no unrelated change entered production

Do NOT claim full enterprise production readiness merely because the technical pilot can start.

Verifier/device enrollment remains an onboarding control to be completed with the pilot customer.

RETURN ONLY

STATUS:
PASS / FAIL

RELEASE COMMIT:
<sha>

FILES COMMITTED:
<list>

PUSH:
PASS / FAIL

DEPLOY:
PASS / FAIL

GITHUB HEAD:
<sha>

EXPECTED_GIT_COMMIT:
<sha>

DEPLOYMENT SHA:
<sha>

RUNTIME SHA:
<sha>

RUNTIME STAMP:
<sha>

SHA CHAIN MATCH:
YES / NO

POLICY HASH MATCH:
YES / NO

REGISTRY HASH MATCH:
YES / NO

EVIDENCE HASH MATCH:
YES / NO

PRODUCTION PILOT BADGE:
<value>

PRODUCTION VERIFIER BADGE:
<value>

PRODUCTION SYNC BADGE:
<value>

FAIL-CLOSED PRESERVED:
YES / NO

EXECUTION BLOCKED WHEN NOT READY:
YES / NO

NEGATIVE TRUTH TESTS:
<passed/failed>

ROLLBACK READY:
YES / NO

ROLLBACK REFERENCE:
<sha/reference>

SECRET LEAKAGE:
YES / NO

AUDIT EVIDENCE:
<path/reference>

REMAINING GAPS:
<numbered list or NONE>

ENTERPRISE PILOT READY:
YES / NO

FULL ENTERPRISE PRODUCTION READY:
YES / NO