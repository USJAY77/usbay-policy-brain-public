PB-RUNTIME-008 — RUNTIME HEALTH CROSS-LAYER EVIDENCE LINKAGE

WORKSPACE: REPLIT AGENT ONLY
REPOSITORY: USBAY Governance Gateway / Runtime Workspace

GOAL

Link Runtime Health evidence to the wider USBAY governance evidence chain so every /execute decision can be traced across runtime state, policy profile, decision outcome, audit hash, and upstream governance context.

RULES

- One governance capability only.
- Replit workspace only.
- No Codex adapter work.
- No simulator changes.
- No travel/voucher changes.
- No tenant changes.
- No RFC3161 changes.
- Preserve fail-closed behavior.
- Do not touch unrelated dirty files.
- Produce audit evidence before remediation summary.
- Do not store raw payloads, signatures, secrets, or raw client identifiers.

BACKGROUND

PB-RUNTIME-007 completed:
- runtime health evidence integrity validation
- SHA-256 hash-chained audit envelopes
- decision_id validation
- no-sensitive-data validation
- tamper detection
- 115 gateway tests passing
- 74 voucher/policy tests passing

TASKS

1. Add cross-layer evidence linkage fields to Runtime Health audit records:
   - decision_id
   - runtime_health_state
   - runtime_health_profile
   - profile_reason_code
   - execution_allowed
   - audit_event_type
   - audit_hash
   - previous_audit_hash
   - governance_context_id
   - policy_context_id if available
   - gateway_context_id if available

2. If policy_context_id or gateway_context_id is not currently available:
   - document as GAP
   - do not invent fake IDs
   - keep governance_context_id deterministic and non-sensitive

3. Add validation proving:
   - runtime evidence links to a governance context
   - missing governance_context_id fails evidence validation
   - mismatched decision_id/audit_hash fails validation
   - hash chain remains valid after linkage
   - no raw sensitive data is introduced

4. Produce cross-layer evidence audit documentation.

SCOPE

gateway/app.py
tests/test_gateway_app.py
evidence/audit/RUNTIME_HEALTH_CROSS_LAYER_EVIDENCE_AUDIT.md

VALIDATION

- python3.11 -m py_compile gateway/app.py tests/test_gateway_app.py passes
- pytest -q tests/test_gateway_app.py passes
- voucher/approval suites pass if present
- git diff --check passes
- git diff --cached --check passes
- runtime evidence has governance_context_id
- incomplete linkage fails validation
- hash chain remains valid
- no raw sensitive data in audit logs

REPORT

Return:

- exact files changed
- validation results
- linkage fields added
- hash-chain status
- documented GAPs
- fail-closed evidence
- rollback command

DELIVERABLE

Every Runtime Health /execute audit record must be linked to a deterministic governance context without leaking sensitive data or weakening the existing fail-closed path.