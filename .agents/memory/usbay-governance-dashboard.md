---
name: USBAY governance dashboard conventions
description: Naming reality, preview-section pattern, and test-runner quirks for the single-file FastAPI gateway
---

# USBAY governance dashboard

## "Euria" branding does not exist — product is USBAY
Users repeatedly ask to verify/render "Euria" fields (e.g. `authority_euria`, "Euria Analysis ID"). The codebase has zero "Euria" references; everything is USBAY-namespaced. When a request names "Euria", confirm whether they want a net-new feature vs. the wrong project — do NOT assume an "API mapping bug."
**Why:** Avoids building on a false premise. A user did confirm they wanted "Euria" built as a brand-new preview-only UI layer on top of USBAY.

## Preview-only demo sections: two homes, two %-rules
Two distinct injection sites for preview-only UI. Both are client-side only (no fetch/backend), self-contained (scoped CSS prefix + IIFE), and must NOT touch governance logic.
1. **`_simulator_block_html()`** (grep that def) — injected at top of `<main>` on BOTH `/` and `/playground`. It returns a PLAIN triple-quoted string with NO Python `%`-formatting → use SINGLE `%` in CSS (do NOT write `%%`). The intake modal ("Start Governance Assessment" → "Generate preview" submit id=`usbsim-intake-submit`, handler `generatePreview()`) lives here.
2. **`playground_html()`** page builder — runs through Python `%`-formatting → escape every literal `%` as `%%`. Playground-only sections (e.g. demo-readiness) go here, before the runtime-telemetry `strip`.
Always `esc()`/`garEsc()` all dynamic JS values regardless of site.

## Governance Assessment Result is wired to "Generate preview", not a standalone button
The Governance Assessment Result panel (id=`usbsim-gar`) is rendered by `generatePreview()` inside the intake preview output (`#usbsim-intake-out`, itself inside the intake modal). Because it lives in `_simulator_block_html()`, it shows on both `/` and `/playground`. A prior standalone `#usbsim-assess` section (playground-only, own "Start Governance Assessment" run button) was removed to avoid a duplicate panel.
**Why:** user reported the panel "did not render" because they were on `/` where only the simulator block exists; the standalone panel was playground-only. Consolidating into the Generate-preview flow fixes both pages with one source of truth.
**How to apply:** outcome model lives in `GAR_DATA` (ALLOW/BLOCKED/HUMAN_REVIEW/FAIL_CLOSED → rec/sig/ts/euria/usbay/human); toggle chips re-render; IDs are random hex (`garHex`), illustrative only.

## Test runner quirk
Repo has ~150 test files (~1641 collected tests); whole-repo `pytest` exits `-1`/hangs in the sandbox — run per-file/per-target or per-marker (`-m critical`, `-m governance`). No `pytest-xdist` (`-n` unsupported). Some files hang on missing services (`test_redis_store`, `test_hydra_consensus`) — run lighter files individually with `timeout`. Gateway-relevant suites: `tests/test_gateway_app.py` (37 tests, all pass), `tests/test_governance_dashboard.py`. `./signed_test.py` is a live-network test that fails on SSL verification offline — pre-existing, unrelated.

## Pre-existing openssl Ed25519 test failures (sandbox env, NOT your change)
In the sandbox, the `critical` + `governance` lanes have ~5 pre-existing failures rooted in openssl Ed25519 signing/verification: `pkeyutl: Option unknown option -rawin` and `SystemExit: EVIDENCE_ED25519_SIGN_FAILED`. Affected: `tests/test_governance_validation.py` (committed-policy signature verify / fail-closed-on-changed-policy), `tests/test_production_readiness.py` (CI evidence manifest signature tests). Source: `runtime/policy_validator.py:~222`, `scripts/generate_ci_evidence_manifest.py:~824`.
**Why:** the sandbox openssl build lacks `-rawin` (raw Ed25519). These are environment-blocked, unrelated to any UI/simulator change — do NOT try to "fix" them when verifying a frontend task.

## Dev-preview 502 — root cause + fix
Bare dev domain returned 502 while `:5000` was 200, because `.replit` `[[ports]]` pins externalPort 80 -> localPort 8765 (stale; nothing listens there) while the app runs on 5000. `.replit` cannot be edited directly (platform-protected) and `configureWorkflow` only sets the workflow's own `waitForPort` (5000) — it does NOT reassign the stale externalPort, and a hard restart does not reconcile a pinned `[[ports]]` entry.
**Fix:** added `scripts/preview_port_forward.py` (stdlib asyncio TCP relay 0.0.0.0:8765 -> 127.0.0.1:5000) and run it backgrounded in the workflow command before uvicorn. This makes externalPort 80 reach the app, so bare `/` and `/playground` return 200. Production unaffected (uses `$PORT`).
**Why a relay, not a .replit edit:** direct `.replit` edits are blocked and no exposed tool rewrites externalPort; the relay is the minimal, reversible, governance-logic-free way to satisfy the existing 80->8765 mapping. The workspace preview pane / screenshot tooling also expect the app on port 80, so this fixes those too.

## Wizard checklist "bags" must be cleared in place, never reassigned
The Governance Pilot Wizard stores per-item toggle state in shared objects (`wizChecks.milestones/evidence/approvals/criteria`). `wizBindChecks()` adds ONE delegated `change` listener per container that closes over the bag object reference. Render/read (`wizRenderChecks`, `wizSelected`) read from `wizChecks.<bag>`.
**Why:** Reassigning a bag (`wizChecks.evidence = {}`) on restart / intake-handoff orphans the listener — it keeps writing to the old object while renders read the new one, so toggles silently stop persisting. Fix is `wizResetChecks()` → `wizClearBag()` which `delete`s keys in place, preserving object identity.
**How to apply:** any new wizard checklist must (1) bind once via `wizBindChecks`, (2) reset only by clearing in place. Adding a new bag means adding it to `wizResetChecks()` AND to the `wizBindChecks` calls at init.

## Governance Pilot Wizard is 9 steps and bridges from the assessment
Flow: assessment modal ("Generate preview") shows recommended license → "Continue to Governance Pilot" button (`#usbwiz-from-intake`) copies assessment form fields into `#usbwiz-form`, closes intake, and `openWiz(3)` jumps to Scope. Wizard steps: 1 Assessment, 2 License, 3 Scope, 4 Objectives, 5 Evidence, 6 Approvals, 7 Criteria, 8 Timeline, 9 Summary. CTA label is gated on `WIZ_MAX-1`/`WIZ_MAX`, not hardcoded step numbers.
**Why:** user wants the license recommendation kept as the bridge between assessment and pilot (no payment/booking framing). Assessment and wizard share identical form field `name`s, so prefill is a plain value/checkbox copy.

## Recommended Pilot Engagement section (assessment result) — capture + summary
A second capture+generate block lives after the Recommended License card (`#usbsim-pileng`, form `#usbeng-form`: company/industry/country/challenge/aisystems/regulatory; button `#usbeng-generate`; output `#usbeng-summary`). It follows the GAR pattern of a self-contained inline `<style>` with scoped `usbeng-*` classes. `prefillEngagement(industry, usage, opts)` is called in `generatePreview` right after `applyLicensingFromAssessment` and overwrites the 3 derived fields (industry/aisystems/regulatory via INDUSTRY_LABELS/USAGE_LABELS/REG_LABELS) + hides the stale summary; company/country/challenge are never auto-filled. `generateIntakeSummary()` builds the summary via innerHTML but escapes every dynamic value with a local `esc()` helper, and reads the recommended license + posture from existing `#intake-lic-*`/`#intake-dim-*` textContent (illustrative, like buildDemoSummary).
**Why:** UI-only demo — no payment, no account, no governance-logic change; summary is a client-side illustrative artifact.
**How to apply:** any new field added to the summary HTML MUST go through `esc()`. USAGE_LABELS/REG_LABELS were added near the intake license vars (no equivalent existed before).

## Executive Governance Report ↔ wizard share state via an outer-scope `wizApi`, not DOM-scraping
The single Executive Report modal (`#usbsim-rpt`, opened by `#usbsim-rpt-open`) and `buildReport()` live in the OUTER simulator IIFE scope; the wizard's compute helpers (`wizCollect`/`wizSelected`/`wizChecks`/`wiz*Defaults`/`wizScopeText`/`wizAiSystems`/`wizTimeline`/`wizLicKey`) are defined inside the INNER `if (wiz) {…}` block and are NOT reachable from the outer scope. Pattern: declare `var wizApi = null;` in the outer scope and assign it (object of those helper refs) inside `if (wiz)` just before `wizShow()`. `buildReport()` then computes EVERYTHING from one `s = wizApi.collect()` snapshot (license/maturity%/risk/scope/objectives/evidence/approvals/criteria/timeline), giving a true single source of truth that matches the wizard plan. Engagement-only fields (company/country/challenge/regulatory/aisystems) are read from `#usbeng-form`; gaps are recomputed locally via `reportGaps(s)` mirroring the assessment axis logic (MAT/REV/AUD/ENF_LABELS, top-3 lowest value/max). Guard every wizApi use with `if (s)`/null fallbacks since wizApi is null until the wizard block initializes.
**Why:** reading rendered wizard DOM is fragile (only populated for visited steps); the shared-helpers approach keeps the report consistent with the wizard regardless of navigation and avoids duplicating governance logic.
**How to apply:** DOM is always readable cross-scope, but inner FUNCTIONS are not — expose them via an outer var. Escape all dynamic values: scalars via `textContent` (`setT`), list/kv innerHTML via local `esc()`. Report sections use `usbsim-rpt-*` classes; current ids are `rpt-exec-*`/`rpt-challenge-*`/`rpt-lic-*`/`rpt-risk-*`/`rpt-gaps`/`rpt-scope-kv`/`rpt-objectives`/`rpt-approvals`/`rpt-evidence`/`rpt-criteria`/`rpt-timeline`/`rpt-next-*`.

## Executive Report is EXACTLY 12 numbered sections — user-confirmed order (kept Risk Summary AND Gaps)
The board report's canonical, user-confirmed section order is: 1 Executive summary, 2 Governance challenge, 3 Recommended license, 4 Governance risk summary, 5 Governance gaps, 6 Pilot scope, 7 Governance objectives, 8 Human oversight requirements, 9 Evidence requirements, 10 Success criteria, 11 Pilot timeline, 12 Recommended next step. A non-numbered "For your board" audience block stays at the top, unnumbered. "Governance objectives" (#rpt-objectives) renders the wizard milestones: `setList('#rpt-objectives', s ? wizApi.selected(wizApi.milestoneDefaults(s), wizApi.checks.milestones) : null)` — same source as the wizard plan's "Governance objectives" block.
**Why:** the report briefly went to a 10-section variant (Exec Summary + Gaps dropped), then the user reverted to the full 12 sections, KEEPING Executive Summary, Governance Gaps, AND Governance risk summary. Both `reportGaps(s)` and the `rpt-exec-*`/`rpt-gaps` ids are live again — do not remove them without an explicit request. Single source of truth chain: Assessment → License → Pilot Wizard → Executive Governance Report.
**How to apply:** when editing report sections keep heading numeric prefixes (`N · …` in HTML body) AND the `// N · …` build comments in `buildReport()` in lockstep 1..12. No PDF/export/download — preview-only modal by design.

## "Copy Executive Report" button reads rendered DOM, not the snapshot
Footer button `#usbsim-rpt-copy` (in `.usbsim-rpt-footbtns` next to Close) copies a plain-text recap via `buildReportText()`, which walks the RENDERED report DOM (`.usbsim-rpt-body > .usbsim-rpt-sec`: `.usbsim-rpt-sec-h` headings, `p.usbsim-rpt-v`, `ul.usbsim-rpt-list>li`, and kv lists `.usbsim-rpt-kvk/.kvv` + audience `.usbsim-rpt-audk/.audv`). Uses `navigator.clipboard.writeText` with a hidden-`<textarea>` + `document.execCommand('copy')` fallback; `flashRptCopy(ok)` shows transient confirm/fail text (`.usbsim-rpt-copy-ok` green class) and reverts after ~1.8s.
**Why:** copying from the live DOM guarantees the text matches exactly what is on screen (consistent with the single-source-of-truth principle) and survives section re-specs without separate maintenance.
**How to apply:** if you add a new section markup pattern/class to the report, extend `buildReportText()`'s selectors or it will be silently omitted from the copy.

## Governance approval is a SEPARATE Pilot Request intake modal, NOT a wizard step
Governance approval was moved OUT of the wizard (wizard is back to 9 steps; all `wizApproval*`/`WIZ_APPR`/`usbwiz-appr-*`/step-10 code deleted). It now lives in a dedicated **Pilot Request → Governance Approval** modal (`#usbpr`, body `#usbpr-body`) opened by a "Submit Pilot Request →" button (`#usbsim-rpt-pilot`) in the Executive Report footer (Copy/Close demoted to ghost). Clicking it does `closeRpt(); openPr()`. The modal reuses the `.usbsim-rpt-*` shell + repurposed `.usbpr-*` CSS (renamed from the old `usbwiz-appr-*`). All `pr*` JS lives in the OUTER simulator IIFE scope (alongside `buildReport`/`wizApi`), state in `var prReq = {name,org,role,id,ts,status,submitted,history}`; statuses in `PR_APPR` map.
**Requester identity (fail-closed):** the DRAFT modal shows a requester form (Name, Organization [prefilled from `#usbeng-form` company, never asked twice], Role) — `prAction('submit')` requires ALL THREE non-empty or it blocks with `#usbpr-err` + focus. A pilot CANNOT be submitted without a named accountable requester. After submit the form seals into a read-only KV. Requester identity appears in the Pilot Request modal, the approval review body, AND the Executive Report exec KV (`buildReport` pushes Accountable requester/Requester role/Requester organization/Pilot Request id+status onto `execKv` when `prReq.submitted`).
**State machine:** DRAFT → SUBMITTED → PENDING_GOVERNANCE_REVIEW → {APPROVED | HUMAN_REVIEW_REQUIRED} → PILOT_ACCEPTED, with REJECTED/reset branches. PILOT_ACCEPTED is reachable ONLY via an explicit human "Accept Pilot (human approver)" action from APPROVED/HUMAN_REVIEW_REQUIRED — never directly from governance "approve". Buttons are gated by status in `prButtons()`. Everything else (license/risk/evidence/approvals/timeline/challenge/risk drivers) derives from `wizApi.collect()` — same single-source chain as the report.
**Why:** user asked to convert the pilot flow into a governed intake process where approval is a post-report request step with captured accountable-requester identity (preview-only; USBAY=ENFORCEMENT_AUTHORITY, Euria=ANALYSIS_ONLY label only, human approval MANDATORY, fail-closed DEFAULT). Nothing stored/transmitted — all client-side.
**How to apply:** `wizApi` now also exposes `challengeText` + `riskDrivers` (added so the PR modal can reproduce report content without DOM-scraping). Escape all dynamic values via `prEsc`. Guard `wizApi` use with `if (s)` since it's null until the wizard block inits.

## Pilot intake is a multi-request QUEUE — `prReq` is one record, `prQueue[]` holds many
The single-record model was upgraded to a session queue (user wants reviewers to work through several pilot requests). `prReq` is no longer a singleton literal: it's `prNewDraft()` (adds a `snap:null` field) and there's an outer-scope `var prQueue=[]`. On submit, `prReq.snap=prSnapshot(org)` captures license/risk/industry/company/country/regulatory/aiSystems/challenge/riskDrivers/evidence/approvals/timeline AT submit time, then `prQueue.push(prReq)`. `prReset()` (the "Start a new pilot request" action) now does `prReq=prNewDraft()` so the old record STAYS in the queue. `prRender()` reads from `prReq.snap` when `submitted` (sealed), else live `prSnapshot(prReq.org)` for the draft preview.
**Queue modal** `#usbpq` (reviewer view, opened by `#usbsim-rpt-queue` ghost btn in report footer AND by the `data-pr="queue"` "View in Intake Queue →" btn appended in `prButtons()` for every submitted/terminal state). `pqRender()` shows stats (`pqStat()`) + a `.usbpq-table` of all queued requests (ID/Requester/Org/Role/License/Risk/Submitted/Status badge + a `data-pq="<idx>"` Review btn). Clicking Review → `closePq(); openPrRecord(prQueue[idx])` which sets `prReq=rec; openPr()`.
**Shared-reference is intentional:** `prQueue.push(prReq)` stores the same object identity, so reviewer status/history mutations on the opened record reflect in the queue. Reset rebinds `prReq` to a fresh object, leaving queued records intact — no corruption path. Status badges reuse `.usbpr-status s-*` via `PR_APPR[r.status].cls`.
**Why:** preview-only governance simulation demonstrating intake/prioritization/review/approval across multiple requests; session-only memory (no storage/persistence/transmission). Architect review PASSED (no blocking issues).
**How to apply:** all queue values escaped via `prEsc`; the `<idx>` in `data-pq` is internally generated. CSS is `.usbpq-*` (added before the 640px media query) + `.usbpr-act.act-queue`.

**Queue triad — filter bar + legend + per-status breakdown:** `PQ_META` (7 entries `{k,short,legend}`, defined order DRAFT→SUBMITTED→PENDING_GOVERNANCE_REVIEW→HUMAN_REVIEW_REQUIRED→APPROVED→REJECTED→PILOT_ACCEPTED) is the SINGLE source for all three; reuse it, don't re-list statuses. `pqStat()` returns a per-status count map (replaced the old grouped {total,pending,...}). `pqRender()` emits: summary chips (total + one count per status, color via `pqCls(k)`=`PR_APPR[k].cls`), a `.usbpq-filters` bar (`data-pqf="ALL"|<status>` buttons, `is-active` on `pqFilter`), the table, then a `.usbpq-legend`. `var pqFilter='ALL'` (outer scope); `openPq()` resets it to ALL. The pqBody click listener resolves `[data-pqf]` FIRST (set pqFilter + re-render + return) BEFORE `[data-pq]` Review. **Index integrity:** filtered rows still use the TRUE `prQueue` index for `data-pq` (loop over full queue, `continue` on non-match) so Review opens the right record. Empty-queue → `.usbpq-empty`; filtered-to-nothing → `.usbpq-nomatch`. **DRAFT chip/count is intentionally always 0** (drafts never enter prQueue) — user explicitly wanted all 7 shown for completeness. Architect review PASSED.

**Queue is now PRE-SEEDED for demos:** `prQueue` init was changed from `[]` to `prSeedQueue()` — 5 sample records (one each: PENDING_GOVERNANCE_REVIEW / HUMAN_REVIEW_REQUIRED / APPROVED / REJECTED / PILOT_ACCEPTED), each a complete record (`name/org/role/id/ts/status/submitted:true/history[]/snap{}`) so the Review modal renders fully. The live Submit-Pilot-Request flow still `prQueue.push(prReq)`, so a new submission appends as the 6th row. DRAFT/SUBMITTED counts stay 0 (terminal/in-review statuses only). Seed records are static literals — no `wizApi`/`prSnapshot` dependency.
**Evidence-screenshot quirk:** the `.usbpq-table` overflows the modal card (last cols Recommended License/Risk/Status/Review get clipped under `.usbpq-tablewrap{overflow:auto}`). To capture all 8 columns in a Playwright shot, inject evidence-only CSS (`page.add_style_tag`) widening `#usbpq .usbsim-rpt-card` + `overflow:visible` on the wrap — DOM-side only, never an app code change.

## Governance Lifecycle Demo — single-screen 9-stage walkthrough (`#usblc`)
Self-contained ADDITIVE section in `_simulator_block_html()`: its own HTML `<section id="usblc">`, an inline scoped `<style>` (`.usblc-*`), and an INDEPENDENT IIFE `<script>` placed AFTER the main simulator `</script>` (its own state `cur`/`stamps`, not wired into wizApi/prQueue). Lets a prospect walk all 9 stages from one screen: clickable rail nodes (`data-lc`) + Prev/Next + Restart, visual progression (`is-done`✓/`is-active` node classes), per-stage status badge (`s-ok|pending|review|human`), accumulating audit trail (entries 0..cur, UTC timestamps generated client-side and cached in `stamps[]`), and always-visible responsibility chips. Stage content is static literals in a `STAGES` array (key/title/short/badge/cls/lead/rows/audit) — NO governance-logic dependency. All rendered values escaped via local `esc()`.
**Why:** combines the lifecycle conceptually into ONE demo without touching the real assessment/wizard/report/PR/queue flows — purely client-side, session-only, nothing stored/persisted. Architect PASSED.
**How to apply:** audit trail intentionally reflects current position (shows 1..cur), not a monotonic visited-history — design choice, not a bug. Stages: 1 Assessment, 2 License, 3 Engagement, 4 Request, 5 Intake Queue, 6 Review, 7 Human Approval, 8 Accepted, 9 Report.

## Multi-Reviewer Governance Approval Simulation (`#usbmr`)
Another ADDITIVE self-contained section in `_simulator_block_html()` (same pattern as `#usblc`): own `<section id="usbmr">`, scoped inline `<style>` (`.usbmr-*`), and INDEPENDENT IIFE `<script>` placed AFTER the lifecycle IIFE script (own state, not wired into wizApi/prQueue/lifecycle). Four reviewer lanes from a `LANES` array (compliance/legal/security/executive); each lane gets three actions (approve/reject/escalate-to-human) from an `ACTS` array. Per-lane status badge, accumulating review audit trail (timestamps cached at decision time), pending/approved/rejected/escalated count chips, always-visible responsibility chips, Reset. All rendered values escaped via local `esc()`.
**Fail-closed outcome precedence (the core logic):** any REJECTED lane → "Rejected — pilot blocked"; else any ESCALATED → "Human review required"; else all four APPROVED (executive lane included via `.every`) → "Approved — governed pilot authorized"; else "Reviews in progress" (PENDING). The executive lane IS the mandatory human gate — all-approved cannot be reached without it.
**Why:** demonstrate enterprise multi-reviewer governance review, preview-only/session-only (in-memory JS, no storage/network). Architect PASSED.
**How to apply:** lane click handler delegates on `[data-mr]`+`[data-act]`. Evidence: `scripts/_evidence_multireviewer.py` drives clicks (reset between scenarios) → 7 PNGs (1 compliance, 2 legal, 3 security, 4 executive, 5 human_review escalate, 6 final_approved, 7 final_rejected); `scripts/_compose_multireviewer.py` → 4-col montage `evidence/multireviewer_journey_full.png`. Same Playwright env rules as the lifecycle scripts apply.

## Playwright evidence in THIS sandbox — three hard-won execution rules
The chromium env is memory/cgroup-fragile. What actually works (CHROME path + no-sandbox/disable-dev-shm/disable-gpu as before):
1. **Run FOREGROUND in a single bash tool call** (`timeout 100 python3 -u <script>`). Background/`nohup &` processes are KILLED when the bash tool call ends (the `&` child shares the tool's process group) → log file vanishes, 0 output.
2. **Do NOT put `pkill -f chromium` in the SAME command that launches chromium** — it reliably OOM-kills the bash shell (exit 137). If you must clean strays, do it in a SEPARATE prior tool call, then verify `ps | grep chrom == 0`, then launch in a clean call with NO pkill.
3. **Avoid `--single-process`** foreground (OOM-kills the shell, 137) and avoid `device_scale_factor=2` + tall viewport (memory spike). Use multi-process, `device_scale_factor=1`, viewport ~1240×1320. For element captures disable transitions (`page.add_style_tag("*{transition:none!important;animation:none!important}")`) + `el.screenshot(animations="disabled")` to dodge "element is not stable".
Scripts: `scripts/_evidence_lifecycle.py` (9 stage PNGs), `scripts/_compose_lifecycle.py` (3×3 montage → `evidence/lifecycle_journey_full.png`). **Pillow montage glyph trap:** source strings use literal `\u2014`/`\u2192`/`\u2022` escapes (the file is ASCII text); the default font renders these as tofu boxes — replace the ESCAPE-SEQUENCE text (`sed 's/\\u2014/-/g'`), not the unicode char, or use ASCII (`-`,`->`,`|`).

## Governance Evidence Package Generator (`#usbep`)
ADDITIVE self-contained section in `_simulator_block_html()` (same pattern as `#usblc`/`#usbmr`): own `<section id="usbep">`, scoped inline `<style>` (`.usbep-*`), and INDEPENDENT IIFE `<script>` placed AFTER the `#usbmr` IIFE script, before the block-closing triple-quote (own state, not wired into wizApi/prQueue). Shows the FINAL post-approval deliverable: a board-ready executive cover (`#usbep-cover`: kicker, title, prepared-for/generated/evidence-id/policy meta, big decision stamp, exec summary/outcome/recommendation, key-controls list, invariants seal) PLUS 10 numbered document cards (`#usbep-doc`: 1 Governance Summary, 2 Pilot Request Information, 3 Risk Assessment, 4 Human Approval Record, 5 Reviewer Decisions, 6 Audit Evidence, 7 Policy Version, 8 Governance Controls Applied, 9 Decision Outcome, 10 Executive Recommendation). Tab switcher (`data-ep` approved/human/rejected) re-renders cover+doc from a static `VARIANTS` object. All values `esc()`'d; IDs/hashes are client-side random hex (illustrative).
**Outcome consistency:** approved→all reviewers APPROVED + human gate APPROVED + green stamp; human→one reviewer ESCALATED/PENDING + human gate PENDING + amber stamp + "awaiting human gate"; rejected→one reviewer REJECTED + human gate NOT REACHED + red stamp + fail-closed "pilot blocked, no execution". Architect PASSED (no severe issues).
**How to apply:** evidence `scripts/_evidence_evidencepkg.py` → 4 PNGs (1_approved/2_human_review/3_rejected full sections + executive_report = `#usbep-cover` element shot); `scripts/_compose_evidencepkg.py` → 3-col journey montage `evidence/evidencepkg_journey_full.png`.

## Element-screenshot of a TALL section bleeds the relative `.topbar` into the capture
When Playwright `element.screenshot()` / `el.screenshot()` captures a section taller than the viewport (captureBeyondViewport stitching), the page's `.topbar` header (and `body::before` fixed bg) bleed into the stitched image and OVERLAP the captured element's top — even though `.topbar` is `position:relative` and sits ABOVE `<main>` in the DOM. Symptom: the section's own top content (e.g. the evidence cover's board title + decision stamp) is hidden behind the app nav/runtime-strip.
**Fix:** before capture, inject evidence-only CSS `page.add_style_tag(content=".topbar{display:none !important;} body::before{display:none !important;}")` (DOM-side only, never an app code change). This is in addition to the transition/animation disable rule.
**Why:** purely a capture artifact of full-element stitching, not a layout bug — hiding the app chrome for the shot is the minimal, non-invasive fix.

## Pilot Readiness Center (`#usbprc`) — dashboard + generated PDF/JSON deliverables
ADDITIVE preview section (own `<section id="usbprc">`, scoped `.usbprc-*` `<style>`, INDEPENDENT IIFE placed AFTER the `#usbep` IIFE before the block-closing triple-quote). Renders 10 readiness sections (Governance Controls, Trust Infrastructure, Licensing Status, Independent Assurance, Customer Journey, Reference Architecture, Certification, Deployment, Operational, Remaining Open Risks) as cards + an overall posture banner + invariants chips. Status is computed CLIENT-SIDE from a static `SECTIONS` literal where each evidence item is `[label, path, present]`; `statusOf` → READY (0 missing) / PARTIALLY_READY (some) / NOT_READY (all missing); `rollUp` → NOT_READY > PARTIALLY_READY > READY. All dynamic text `esc()`'d.
**Single source of truth = `scripts/generate_pilot_readiness.py`** (uses `fpdf2`). It maps the SAME 10 sections to real repo paths, verifies `os.path.exists`, and writes BOTH `pilot_readiness_manifest.json` + `pilot_readiness_report.pdf` at repo root with identical fail-closed logic. The dashboard literals are hand-mirrored from the generator output — **if evidence mapping changes, re-run the generator AND update the dashboard `SECTIONS` literal in lockstep.** Current truthful result: overall PARTIALLY_READY, 25/28 evidence present; 3 CI-built artifacts genuinely missing → sections 7/8/10 PARTIALLY_READY (`evidence/governance-evidence-manifest.json`, `governance_release.json`, `evidence/governance-provenance.json`).
**fpdf2 quirks:** core fonts are latin-1 → run all text through an `_ascii()` helper (no em-dash/arrows/unicode). `ln=1` is deprecated → use `new_x=XPos.LMARGIN, new_y=YPos.NEXT`. Cell x-position DRIFTS across rows (multi_cell default `new_x` is LEFT-of-cell, not margin) → explicitly `pdf.set_x(16)` before each evidence row or width shrinks until "Not enough horizontal space to render a single character".
**Architect false-positives to pre-empt:** an evaluate_task review WILL flag (a) "editing `gateway/app.py` violates 'do not modify Enforcement Gateway'" and (b) "writing PDF/JSON violates 'session-only/nothing persisted'". BOTH are wrong: `gateway/app.py` is the PREVIEW HOST app (not the governed Enforcement Gateway runtime) and the task explicitly REQUIRES the JSON+PDF as deliverables; session-only governs the dashboard's runtime data handling, not build outputs. Don't tear apart correct work over these — substantive checks (fail-closed logic, literal/manifest match, XSS) all PASSED.
**Screenshot:** `scripts/_shot_usbprc.py` (same Playwright env rules) → `evidence/pilot_readiness_dashboard.png`.

## Governance Simulator is a STANDALONE `/simulator` route — NOT a `_simulator_block_html()` preview section
Unlike `#usblc`/`#usbmr`/`#usbep`/`#usbprc` (which inject into `_simulator_block_html()` shared by `/` and `/playground`), the "USBAY Global Governance Command" SOC console is its OWN full-page route: builder `governance_simulator_html()` (returns a complete `<!doctype html>` doc as a PLAIN triple-quoted string → single `%` in CSS is safe, NO `%%`), and route `@app.api_route("/simulator", ...)` registered AFTER the playground routes but BEFORE the catch-all `/{frontend_path:path}` (catch-all reserves `/api/*`+`health` only, so a specific `/simulator` defined earlier wins). Self-contained client-side IIFE: 5 SCENARIOS (loan/med/gov/av/face) with risk LOW..CRITICAL weights `RW`, `evaluate(scn,action)` returns per-score deltas + token deltas + audit reason + verdict class, scores Trust/Risk/Audit/Runtime clamped 0..100 (Risk higher=worse), tokens GOV/AUDIT/Reputation, decision_id `GOV-YYYYMMDD-<hex6>`, audit log unshift. Governance-aligned scoring: APPROVE on HIGH/CRITICAL = "Governance breach" (penalized); BLOCK on high = fail-closed reward; HUMAN_REVIEW = always strong audit gain. Nav link `<a href="/simulator">Governance Simulator</a>` added additively to BOTH the gateway topnav (line ~8420, `${}` Template page — link has no `$`) and the playground breadcrumb nav (line ~8826, `%`-formatted page — link has no `%`).
**Why:** task USBAY-GOV-SIM-001 required a NEW standalone page that must NOT replace/break `/health`, `/api/status`, `/api/governance/evidence`, `/`, `/playground`. Standalone = zero risk to control plane. Architect PASSED.
**How to apply:** evidence `scripts/_shot_simulator.py` (foreground `timeout 100 python3 -u`, drives clicks, full_page shot → `evidence/governance_simulator.png`). All dynamic JS values `esc()`'d. Demo/training only — no crypto/money/multiplayer/external-API, nothing stored.

## Standalone /simulator persists to localStorage — sanitize on load (DOM XSS class-attr trap)
The standalone `/simulator` (`governance_simulator_html()`, separate from `_simulator_block_html()`) persists its whole `state` (scores/tokens/profile/decision-log) to `localStorage` key `usbay_sim_state_v2`. Player profile = operator name + rank (Trainee→Operator@3→Specialist@7→Architect@12→Sovereign Controller@20) + level (XP/100+1) + completed + streak (consecutive sound = cls ok|human). Export Audit JSON via Blob download; Reset behind a confirm modal.
**Why:** A persisted audit entry's `cls` was interpolated UNescaped into HTML `class="audit a-"+e.cls` — `esc()` only guards text nodes, NOT attribute-context class tokens. Tampered localStorage → DOM XSS. Architect flagged this as the one blocking issue.
**How to apply:** any value put into a class/attribute by string-concat needs an ALLOWLIST, not `esc()`. Fix used `safeCls()` (allowlist ok|warn|bad|human→else warn) at render AND `normalizeLogEntry()` on load (allowlist action/risk/result, coerce others via `str()`/`num()`). Persistence FAILS CLOSED: a write/read/probe try-catch sets `persistOK=false` + shows `#persist-banner`. Validation: `scripts/_shot_simulator_v2.py` (22 checks), `scripts/_shot_simulator_xss.py` (hostile-localStorage negative test).

## Standalone /simulator was extended into a governance TRAINING GAME (gamification layer)
`governance_simulator_html()` gained five ADDITIVE client-side systems (credits, reputation composite, daily/weekly/special missions, tiered scenario unlocks, local leaderboard) WITHOUT touching control-plane routes. Durable invariants to preserve:
- **Fail-closed unlock is enforced in THREE places, not just the modal.** A locked scenario must be unplayable even if `localStorage` (`usbay_sim_state_v2`, `idx` field) is tampered. Authorization is checked at: (1) `load()` re-validates `idx` AFTER `unlocked` is populated and clamps to `firstUnlockedIdx()`; (2) `select()` opens the unlock modal instead of selecting a locked scenario; (3) `decide()` hard-returns with a fail-closed verdict if the current scenario isn't unlocked. **Why:** the modal alone is a UI gate, not a security boundary — only the `decide()`/`load()` backstops actually prevent the bypass. Any new "gated content" must guard the ACTION path, not just the open-dialog path.
- **The "global leaderboard / global sync" button is intentionally enabled but FAILS CLOSED** — it shows a warning and makes zero network calls. Do not "implement" it into a real fetch; the no-network fail-closed behaviour is the requirement.
- **No crypto / gambling / cash-rewards / multiplayer / external API.** Credits are a training economy only; everything is session-or-localStorage, nothing transmitted.
- **Per-tier cost lives on the SCENARIO object (`s.cost`), not the `TIERS` map** (TIERS maps tier→label string only). Reading `TIERS[tier].cost` silently yields 0.
- **SIM_VERSION and the `_shot_simulator_v2.py` version assertion move in lockstep** (currently `usbay-sim-3.0`); `LS_KEY` stays `usbay_sim_state_v2` and `load()` sanitizers migrate old state forward.
- **Renderer-emitted class names and the CSS block must match** — mismatches render invisibly and Playwright text assertions still pass, so verify gamification UI with an actual screenshot. Reuses existing `.bar.good/.bar.risk` + `.log-empty`.
**How to apply:** validation suite is `scripts/_shot_simulator_v3.py` (incl. an in-bounds tampered-`idx` bypass negative test) alongside `_shot_simulator_v2.py` + `_shot_simulator_xss.py`. Same Playwright env rules as the other sim scripts.

## Simulator long-term progression layer — 5 additive systems on `governance_simulator_html()` (`/simulator`)
The `/simulator` endpoint (distinct from `_simulator_block_html()` on `/` and `/playground`) is its own single self-contained HTML+CSS+JS string with a MAIN IIFE (state `state`, `LS_KEY='usbay_sim_state_v2'`, `SIM_VERSION='usbay-sim-4.0'`). Five additive systems coexist there: (1) Career Ladder `RANKS` array (8 tiers Trainee→Chief Governance Officer, `rankFor`/`renderTrack` → `.pf-track` of `.trk`/`.trk-n` cells); (2) Reputation Engine `state.scores` has 5 axes incl. `oversight` — `evaluate()` sets per-branch deltas, `decide()` clamps, `repIndex`/`governanceScore` weight them, `renderReputation` → 4 `.rep-cell` (`.rep-k`/`.rep-v`); (3) Enterprise Scenario Packs (`PACKS` 6 sectors, each scenario tagged `pack`, `#pk-grid` `.pk-card[data-pack]` click→`selectPack` jumps to first scenario or opens unlock fail-closed); (4) Team Mode (`TEAM` seeded roster+history + live "you" row in `#tm-roster`, aggregate `#tm-score`/`#tm-rep`, `#tm-hist`); (5) Persistence facade (`LocalStorageAdapter` + fail-closed `RemoteApiAdapter` stub + `persistence` get/set/remove/syncRemote; `#persist` shows adapter/remote/mode; Sync stays NOT_CONFIGURED, transmits nothing).
**Why:** turns the one-shot simulator into a long-term governance progression platform without touching control-plane routes (`/`,`/dashboard`,`/playground`,`/health`,`/api/*`) — all client-side, fail-closed, training-only (no crypto/gambling/real-money/marketplace). Architect PASSED.
**How to apply:** the app does NOT write localStorage until the first `save()` (first decision/unlock) — tests must read live values from the DOM (e.g. `.rep-cell` text) or trigger a decision first, not from `localStorage` on load. `state` has NO `version` field; version lives only in the EXPORT JSON (`simulator_version`, via `#btn-export` download). Decision buttons are `.btns [data-act="APPROVE|BLOCK|HUMAN_REVIEW"]`; scenario rail is `#scn-rail`. All new renderers escape via `esc()`/`safeCls()`. Test suites: `scripts/_shot_simulator_v2.py` (22), `_v3.py` (26), `_v4.py` (27 new-systems), `_xss.py`.

## Playwright chromium in this sandbox — the hardcoded `/nix/store/...chromium` path is VOLATILE
Test scripts that hardcode `CHROME = "/nix/store/<hash>-chromium-.../bin/chromium"` break whenever the nix store is regenerated (the hash changes); `playwright install chromium`'s bundled browser also fails with `libnspr4.so: cannot open shared object file` because it lacks nix-provided system libs.
**Why:** the bundled Playwright chromium is not wired to the nix lib closure; only the nix-packaged chromium is self-contained.
**How to apply:** install the self-contained browser via `installSystemDependencies(["chromium"])`, find it with `which chromium`, and pass it as `executable_path`. Make scripts resilient: `p.chromium.launch(executable_path=os.environ.get("CHROME") or p.chromium.executable_path, ...)` and run with `CHROME=$(which chromium) python3 -u <script>`. Same foreground/no-pkill/multi-process execution rules as the other evidence scripts apply.

## Academy v5 (`usbay-sim-5.0`) — persistent backend + leaderboard scope/teams
The `/simulator` console (`governance_simulator_html`, distinct from the `_simulator_block_html` preview sections) gained a REAL persistence backend, all ADDITIVE and namespaced under `/simulator` — never the control plane (`/`,`/dashboard`,`/playground`,`/health`,`/api/*`).
- Backend lives in its OWN package `simulator/` (`storage.py`, zero import dep on gateway): `StorageAdapter` ABC + local-file/sqlite/postgres adapters, `get_storage_adapter()` reads `USBAY_SIM_STORAGE_BACKEND` (default `local`; default dir `tmp/usbay_sim_storage`, gitignored). Postgres with no DSN raises `StorageUnavailable` (fail-closed). Key/value validated + size-capped; SQL parameterized.
- Endpoints in gateway: `GET /simulator/storage/health`, `GET/PUT/DELETE /simulator/state/{client_id}` — client_id slug-validated, payload size-limited, opaque-JSON only, fail-closed on any error.
- Frontend: `SIM_VERSION` bumped 4.0→5.0 but **`LS_KEY` stays `usbay_sim_state_v2`** (do not bump — would orphan saved state). New `CID_KEY` keys the backend client id. RemoteApiAdapter is now a WORKING round-trip (health/push/pull, async `syncRemote`); localStorage stays PRIMARY fail-closed fallback so a dead backend never blocks play. New state: `profile.country/org`, `credits.sources{incidents,missions,audit}`, `team{name,org,created}`, leaderboard entry `country/org`; normalizers `normCredits/normTeam/normLbEntry`.
**Contract-copy lockstep (architect caught this):** once the simulator actually transmits state to the backend, EVERY user-facing string claiming "never transmitted / nothing is transmitted / no network sync" becomes a false contract and MUST be updated in the same change (lede, `ac-note`, `tm-note`, `ps-note`, footer). The leaderboard "Global sync" button (`lb-global`) is the ONE thing still fully fail-closed/local-only — its copy ("sends nothing over the network") stays accurate; don't "fix" it.
**Verification:** `tests/test_simulator_storage.py` (13). Playwright suites `scripts/_shot_simulator_v2..v5.py` + `_xss.py` — when bumping SIM_VERSION, the export-version assertions in v2/v4 must move in lockstep (they hardcode `usbay-sim-N.0`); v5 is the comprehensive 25-check suite. Same foreground/no-pkill chromium rules as the evidence scripts.

## Travel Rewards Readiness (`#travel`) — preview-only eligibility layer in the SIMULATOR
ADDITIVE read-only layer inside `governance_simulator_html()` (the `/simulator` Academy console, NOT the `_simulator_block_html()` preview block). Five partner tiles (airline/train/bus/cruise/hotel) + voucher model + exact fail-closed disclaimer + audit-evidence table. Wired into `renderAll()`, `decide()`, `claimMission()`, and the id-save handler via `renderTravel()`.
**Tamper invariant (the whole point):** `evalTravelVoucher(partnerType, opts)` derives eligibility ONLY from achievements — rank (`rankFor(profile.completed)`), claimed missions, `scores.audit`, `repIndex()` — and NEVER reads `state.credits`. So inflating the credit balance cannot flip eligibility. Keep it that way.
**Status enum is preview_only/eligible/blocked:** missing partner rule → `blocked`+`NO_PARTNER_RULE` (checked FIRST, fail-closed); `opts.preview===true` → `preview_only`+`PREVIEW_ONLY` (non-binding, confers nothing, only for known partners); else eligible/blocked from reqs + expiry (`VOUCHER_EXPIRED` when `expires_at<=now`). Thresholds are tiered so bus is eligible on FRESH default state (rank Trainee, audit 80, rep 75, 0 missions) and the other four are blocked — gives a mixed screenshot.
**Why a `window.__usbayTravel` export:** the eval fn lives in the IIFE; Playwright drives missing-rule/expired/tamper/preview cases via `window.__usbayTravel.evalVoucher(...)` (opts.now/opts.expiresAt/opts.preview overridable). `renderTravel` itself is NOT global.
**How to apply:** CSS lives in a scoped inline `<style>` (`.trv-*`) before the audit-log `<section>`; the `\2713`/`\2717` content escapes must be written `\\2713`/`\\2717` in the Python source (plain triple-quoted string → `\\`→`\`). All dynamic values `esc()`'d. Test: `scripts/_shot_travel_v1.py` (31/31) → `evidence/audit/travel_v1.png` + `travel_panel_v1.png`. No crypto/gambling/cash/stored-value/booking/payment/partner-API; control plane untouched. Architect PASSED after adding the preview_only state.

## Travel layer voucher model — PB-TRAVEL-002 fields, revocation, and lifecycle audit
The `#travel` voucher object (built by `evalTravelVoucher(partnerType, opts)`) carries BOTH `partner_type` AND `partner_id` (same value — partner_id added for the framework contract), plus a boolean `eligible` and a `revoked_at`. `eligible` is fail-closed `false` by default and set `true` ONLY in the single success path (`v.reasons.length===0`) — so it stays false for blocked/preview_only/expired/missing-rule/revoked. When changing the voucher shape, keep `eligible` derived from that one branch, never set it independently.
**Revocation is a terminal fail-closed state** checked BEFORE preview/eligibility: `opts.revoke===true || typeof opts.revokedAt==='number'` → `status:'revoked'`, `eligible:false`, reason `VOUCHER_REVOKED`, `revoked_at` set, returns immediately (confers nothing). Status enum is now preview_only/eligible/blocked/revoked.
**Lifecycle audit evidence:** `travelVoucherLifecycle(v)` derives events from a voucher object — always ISSUANCE; EXPIRATION when `expires_at` set (detail flips on the `VOUCHER_EXPIRED` reason); REVOCATION when `revoked_at` set. `renderTravelLifecycle()` builds the `#trv-life` table from THREE real eval calls (issued/expired/revoked bus voucher) so all three event types are always visible. Exposed for tests as `window.__usbayTravel.lifecycle`.
**Tests:** Playwright `scripts/_shot_travel_v2.py` (36/36) covers the field contract, revocation, lifecycle, fail-closed, credit-tamper, zero-network; pytest `tests/test_travel_voucher.py` (7) asserts server-rendered markers via `TestClient(gateway.app.app)` GET `/simulator` (note: import is `from gateway import app as gateway_app`; control-plane guard test posts `/decide` expecting 4xx). Architect PASSED. Still no payment/booking/partner-API/crypto/cashback; control plane untouched.

## Voucher Redemption Authority — standalone `simulator/voucher.py` + 2 sim endpoints
ADDITIVE, simulator-scoped, fail-closed, NON-MONETARY. New standalone module `simulator/voucher.py` (zero gateway import dep, mirrors `simulator/storage.py` pattern): HMAC-SHA256 sign over a FIXED ordered field set `SIGNED_FIELDS=(voucher_id,client_id,partner_id,issued_at,expires_at,revoked_at)`; constant-time `hmac.compare_digest`; secret from env `USBAY_SIM_VOUCHER_SECRET` (training fallback constant). Public fns: `sign_payload`, `issue_voucher`, `voucher_status` (active/expired/revoked), `verify_voucher`, `redemption_audit_trail`, `make_voucher_id`. Fail-closed reason codes: BAD_SIGNATURE / OWNERSHIP_MISMATCH / MISSING_FIELD / MISSING_SIGNATURE / VOUCHER_EXPIRED / VOUCHER_REVOKED / VOUCHER_ACTIVE. Two gateway routes (additive, simulator-namespaced, NOT control plane): `GET /simulator/voucher/issue` (validates client_id+partner_id slugs, ttl clamp 1-365, `revoke=1` sets revoked_at) and `GET /simulator/voucher/verify` (returns valid/status/reasons/audit_trail, HTTP 200 even on failure). Both responses carry `Cache-Control: no-store` (`_SIM_VOUCHER_NO_STORE`). Frontend "Partner Verification Console" (`#pv-block`) in `_simulator_block_html`; `window.__usbayVoucher{issue,verify,current,tamperCurrent}` bridge for tests.
**Why:** demonstrate signed partner-voucher verification in the training simulator without ANY real-money/payment/booking rails (architect + security both PASSED; 0 voucher-file findings). Expired path is not UI-triggerable (issue always future-dates); covered by pytest only.
**How to apply:** any new signed field MUST be added to `SIGNED_FIELDS` in `simulator/voucher.py` or signatures silently diverge. Banned-substring regression tests (`test_travel_voucher.py`, `_shot_travel_v2.py`, `_shot_voucher_v3.py`) flag the literal token `cashback` ANYWHERE in the rendered page — write disclaimers as "cash-back reward" (hyphen) to avoid false positives. Tests: `tests/test_voucher_authority.py` (pytest), `scripts/_shot_voucher_v3.py` (Playwright, same foreground/CHROME harness rules).

### Central revocation registry (CRL) layer — `RevocationRegistry` in `simulator/voucher.py`
Adds a server-side revocation list on TOP of the signed `revoked_at` field. `RevocationRegistry(storage)` is backed by a `simulator/storage.py` `StorageAdapter` — all revoked vouchers live under ONE key (`REVOCATION_REGISTRY_KEY="voucher_revocations"`) as a JSON object keyed by `voucher_id`. `verify_voucher` gained a keyword `registry=None`: **None = skip the registry (backward-compat for unit tests that call the module directly)**; a registry instance = authoritative, consulted after signature+ownership, overrides status to `revoked`. New status `unavailable` + reason `REVOCATION_REGISTRY_UNAVAILABLE` and a new audit event `revoke_failed`. Gateway route `GET /simulator/voucher/revoke?voucher_id&client_id[&reason&source]` (slug-validates both ids, no-store, idempotent → `already_revoked`). Gateway `_sim_revocation_registry()` is a singleton built from `_sim_voucher_mod()`+`_sim_storage()`; the gateway `/verify` endpoint ALWAYS requires it and fails closed (`REVOCATION_REGISTRY_UNAVAILABLE`) when None — only the bare module default is skip. UI: `#pv-revoke` button + `window.__usbayVoucher.revoke`.
**Why fail-closed nuance:** the verify *endpoint* must enforce the registry (unknown revocation status = reject), but the *module* keeps `registry=None`=skip so the 12 pre-existing pure-unit tests stay valid without a storage backend.
**Tamper trap (architect caught this):** `RevocationRegistry.get_record` must distinguish ABSENT key (→ `None`, legitimately not-revoked) from PRESENT-but-non-dict entry (→ raise `VoucherError` → fail closed). Returning `None` for a malformed present entry is a revocation BYPASS. Top-level corrupt/non-object JSON also raises.
**No-PII rule:** registry records + ALL audit rows store `client_ref(client_id)` (`cref_`+salted-sha256 prefix), NEVER the raw `client_id` (HoundDog = 0 findings confirms). Audit rows carry voucher_id/partner_id/client_ref/status/reason_code/at.

### Governance approval-chain EVIDENCE layer — PB-TRAVEL-005 (additive to voucher lifecycle)
ADDITIVE, simulator-scoped, fail-closed, NON-MONETARY (approval is EVIDENCE ONLY — never money/reward/cashback/crypto/wallet). Layered on top of the signed voucher + CRL in `simulator/voucher.py`. A separate domain-separated HMAC (`APPROVAL_DOMAIN`, distinct from voucher signing) signs an approval attestation bound to the voucher SUBJECT — `APPROVAL_BIND_FIELDS=(voucher_id,client_ref,partner_id,issued_at,expires_at,revoked_at)` — plus a named `approver` and a simulated `approval_ts`. Public fns: `approval_subject`, `_approval_canonical`, `sign_approval`, `make_approval_evidence`, `verify_approval_evidence(payload, evidence, bind_fields=...)`. Reason codes: `APPROVAL_MISSING` (empty dict {} is falsy → MISSING), `APPROVAL_INVALID` (bad/tampered sig or malformed JSON), `APPROVAL_SUBJECT_MISMATCH`, `APPROVAL_EXPIRED` (DEFAULT_APPROVAL_TTL_MS), `TIMESTAMP_MISSING`, `TIMESTAMP_INVALID`. `issue_voucher(with_approval=True)` attaches `approval_evidence` (carries `confers_value:"none"`). `verify_voucher(enforce_approval=False, approval=None)` gates AFTER ownership / BEFORE registry, adds `result["approval"]`; `redemption_audit_trail(approval=None)` emits `approved`/`approval_failed` rows.
**Backward-compat is the core constraint:** module `enforce_approval` DEFAULTS OFF so the pre-existing pure-unit + CRL tests stay green without supplying approval. Gateway endpoints: issue `approval:int=1` (on by default); verify `approval_evidence:str` + `enforce_approval:int=0` (malformed JSON→APPROVAL_INVALID w/ audit row; empty→APPROVAL_MISSING via module); revoke `approval_evidence` + `enforce_approval` (403 on approval failure, bind_fields=("voucher_id","client_ref")). UI: `#pv-noapproval` "Verify w/o approval" button; the console ALWAYS sends `enforce_approval=1`; bridge adds `verifyNoApproval`/`verifyTamperApproval`; approval shown as "governance approval … (evidence only · no money/reward)".
**Why:** demonstrate a governance approval chain as audit EVIDENCE in the voucher lifecycle without introducing any value/reward semantics. Architect PASSED (no critical; optional non-blocking suggestion: add evidence_kind/confers_value to signed canonical fields). security_scan = pre-existing baseline only (1 high dep + 14 high SAST subprocess/secrets in config), 0 findings in voucher files, HoundDog 0.
**How to apply:** any new approval-bound field MUST go into `APPROVAL_BIND_FIELDS` or attestations silently diverge. Tests: ~18 cases appended to `tests/test_voucher_authority.py` (58 pass total w/ travel); `tests/test_travel_voucher.py` adds approval markers + expanded banned list (now also wallet/stored-value/stored_value); `scripts/_shot_voucher_v3.py` (55/55) adds approval scenarios — NOTE enforcing approval means audit trails now include an `approved` row, so existing trail-equality checks were updated to `[issued,viewed,verified,approved(,revoked)]`.

## Runtime governance proof EXPORT (011) → EXPORT INDEX (012) → FRESHNESS INDEX (013)
A backend evidence series in `gateway/app.py` (NOT preview UI; pure functions, read-only, NEVER wired into `/execute`). 013 builds a freshness overlay on the 012 export index classifying each proof package CURRENT/STALE/MISSING/SUPERSEDED/INVALID. Leakage is prevented by WHITELIST record construction (`*_RECORD_HASH_FIELDS`), with `runtime_health_evidence_contains_sensitive_data` as defense-in-depth. Each layer carries forward the prior verdict; overall `valid` is fail-closed (integrity AND export_valid AND hash_chain_valid). Rolling hashes seed from `GENESIS_HASH` and are domain-separated by a per-namespace string.
**Why:** auditors need discoverable/verifiable proof metadata without exposing raw payloads/signatures/secrets/client ids.
**How to apply:** the 012 export index dedupes `decision_id`, so in the chain-derived path every proof is the latest for its decision → SUPERSEDED and MISSING only arise in synthetic/multi-proof tests, not real builds. Test all 5 statuses via the pure `classify_runtime_governance_proof_freshness(...)` helper with explicit reference_time/max_age rather than wall-clock. `proof_status` is intentionally carried on freshness records (a status string, non-sensitive) so INVALID stays independently re-verifiable by the validator.

## Audit envelope `timestamp` is ISO-8601 string in prod but int in test helpers — parse as UTC-aware
`audit/hash_chain.py` stamps `timestamp = datetime.utcnow().isoformat()+"Z"` (a STRING), but the `_proof_entry` test helper uses `timestamp=1` (int). Any time math over proof timestamps must handle BOTH. Critically, `datetime.fromisoformat(s.rstrip("Z")).timestamp()` on a NAIVE datetime is interpreted in the host's LOCAL timezone → non-deterministic epochs across environments.
**Why:** architect flagged this as a blocking determinism bug in 013; the same UTC instant produced different epochs depending on server TZ, which could flip STALE/SUPERSEDED outcomes.
**How to apply:** convert trailing `Z`→`+00:00`, parse, and if `tzinfo is None` set `timezone.utc` before `.timestamp()` (see `_freshness_epoch`). Return None for uninterpretable values and degrade safely (skip age math) rather than guess.

### FRESHNESS INDEX PROOF (014) — compact evidence-only currency proof over the 013 index
ADDITIVE pure functions in `gateway/app.py` (read-only, NEVER wired into `/execute`, does not weaken 013). Summarizes the 013 freshness index into a compact PROOF carrying EXACTLY 11 whitelisted fields (id/status/reason_code/checked_at/max_age/export_index_hash + 4 counts + freshness_index_hash) — no raw payloads/signatures/secrets/client ids. Status precedence INVALID>MISSING>STALE>CURRENT; any non-CURRENT population fails the proof. Deterministic id = `usbafip-`+sha256(namespace|GENESIS_HASH|canonical_json)[:32], bound to every summary field incl. the reference window. Reuses `runtime_health_evidence_contains_sensitive_data` + `_dedupe_reason_codes`.
**Two verification modes (the durable distinction):** WITHOUT the source index, validation only proves structural+schema-exact+non-sensitive+count-consistent+currency-asserting+tamper-evident-id — it does NOT anchor the summary to trusted evidence. WITH the source index it re-derives the expected proof and cross-checks hashes/counts AND status/reason/id — this is the only mode that proves currency.
**Two architect-caught bypasses (both fixed, regression-tested):** (1) a proof forged "healthier" than its index (e.g. CURRENT over INVALID) with a recomputed self-consistent id passed with-index validation until status/reason/id cross-checks vs the index-derived expected were added; (2) the validator accepted extra keys — must enforce the EXACT field whitelist (extra key → INCOMPLETE), else an ungoverned schema extension slips through.
**Why:** USBAY must prove exported governance proofs are current/indexed/non-stale/counted without exposing raw material; self-validation alone is insufficient evidence of currency.
**How to apply:** any new summary field MUST go into the id's canonical payload AND the whitelist AND the with-index expected cross-check, or the proof silently diverges. Tests: 33 cases in `tests/test_gateway_app.py` (`-k afip`); full-suite failures in that file are PRE-EXISTING order/shared-state-dependent (audit chain accumulates) and pass in isolation — not 014 regressions.

## PB-RUNTIME freshness time fields are floats / ISO strings (not ints)
The PB-RUNTIME-013 freshness window uses a FLOAT `freshness_max_age` (e.g. 86400.0),
and `freshness_checked_at` defaults to the latest `proof_generated_at`, which in the
chain-derived path is the production ISO-8601 audit timestamp (a STRING), not a number.
**Why:** any new layer doing staleness math (e.g. PB-RUNTIME-015 regulator evidence
package) that requires `isinstance(x, int)` on these fields will classify everything
INVALID. **How to apply:** normalize time/anchor fields through the shared
`_freshness_epoch` helper (handles int/float/numeric-string/ISO-8601, returns None on
failure) and only require numeric for `max_age`; carry the RAW value so source-index
cross-checks still match. Counts (`evidence_record_count`) stay strict ints.

## runtime_proof_hash / e2e_evidence_hash provenance — 015 GAP, CLOSED by 016
Neither `runtime_proof_hash` nor `e2e_evidence_hash` (PB-E2E-005) is present in the
PB-RUNTIME-013 freshness index — they come from separate authorities. PB-RUNTIME-015
ACCEPTED both caller-supplied and documented this as a GAP. PB-RUNTIME-016
(`*_self_derivation_*`) CLOSES it: `runtime_proof_hash` is DERIVED via
`compute_runtime_proof_hash_from_export(build_runtime_governance_proof_export(chain))`
(namespace `usbay.runtime.proof.hash.v1`) and `e2e_evidence_hash` via
`compute_e2e_evidence_hash(<canonical e2e dict>)` (namespace
`usbay.runtime.e2e.evidence.hash.v1`); export/freshness hashes come from the 013 index.
**How to apply:** prefer DERIVED; accept a caller value ONLY as documented fallback when
the source is genuinely UNAVAILABLE. Each hash carries a provenance source
(DERIVED/CALLER_SUPPLIED/UNAVAILABLE/MISMATCH); overall status precedence MISMATCH >
UNAVAILABLE > CALLER_SUPPLIED > DERIVED. Fail closed on derived/caller conflict, malformed
source, FALSE_DERIVED (DERIVED claim re-derives to nothing — test against an EMPTY chain),
UNDOCUMENTED_FALLBACK (CALLER_SUPPLIED over an available source). 016 reuses the 015 core
builder + validator UNCHANGED on the extracted 12-field core (adds 6 provenance fields =
18-field whitelist); additive, read-only, NOT in /execute. NOTE: `decide_then_execute`
appends MULTIPLE audit records per call (not 1) — assert chain GREW, don't assert +1.

## Regulator evidence self-derivation chain (015→016→017) — two-mode validation pattern
The PB-RUNTIME-015/016/017 regulator-package validators (all in `gateway/app.py`, all ADDITIVE, read-only, NOT wired into `/execute`) share one design: a sealed report binds its fields with a content-derived `*_hash` + `*_id` (sha256 over NAMESPACE|GENESIS_HASH|canonical(fields)), and validation has TWO modes — (1) STANDALONE: structural whitelist + internal consistency + tamper-evidence (recompute hash/id from carried fields) + blocked-state accountability; (2) FULL-EVIDENTIARY: triggered when source evidence is supplied (chain / e2e_evidence / caller hashes / justifications), RE-DERIVES expected values from source and fail-closes on false claims. 017 adds 6 per-source states + an 8-field source-gap-closure report on top of 016.
**Why:** the hash/id binding is tamper-EVIDENCE, NOT provenance/authenticity. A self-consistent forged CLOSED report (states forged + re-sealed) WILL pass standalone — only full mode catches the lie. The architect RELIABLY flags this as an "authenticity gap" and suggests adding a cryptographic signer; that is a FALSE POSITIVE for these tasks — signing is explicitly out of scope (evidence-only, no signatures), and the property mirrors approved 015/016. Document it under "Remaining GAPs" + add a test proving full mode catches the forgery, rather than scope-creeping a signer.
**How to apply:** when a divergence-reason map keys off the EXPECTED state alone, it can mislabel (e.g. emitting MISSING_JUSTIFICATION for any blocked expected state). Disambiguate using the actual input (e.g. is the per-source justification genuinely absent?) so conflict/malformed/sensitive map to STATE_MISMATCH and only true absence maps to MISSING_JUSTIFICATION. Justification TEXT is never stored (only the resulting state) so the report is non-sensitive by construction. Whole-repo pytest exits -1/killed in-sandbox; run per-file and split big suites (e.g. `-k "not gap_"` + `-k gap_`) to a log file.

## USBAY Game (`/game`) — standalone full-page playable prototype (NOT a preview block)
USBAY-GAME-004 is a separate full-page route (`usbay_game_html()` + `@app.api_route("/game")`), inserted just BEFORE `_API_RESERVED_PREFIXES`/the catch-all — same standalone pattern as `/simulator`, NOT a `_simulator_block_html()` preview section. It is a self-contained client-side mini-SPA: data-driven `SCREENS` registry (13 screens), `TRIPS`/`HOTELS`/`LOGI`/`CREW` data arrays, in-memory wallet/scores, VIP discount math centralized in `price(base)` (20% off ALL modes), child-safe relabel via `data-cslabel`, accessibility via `body.a11y`. STRICTLY demo: no fetch/XHR/storage/payment, nothing persisted, no governance-logic touch.
**Why:** the request was a non-production playable demo; keeping it a separate route (not an injected block) keeps it fully isolated from `/` and `/playground` governance UI.
**How to apply:** the route function string uses the plain-triple-quote pattern (single `%` in CSS, no Python `%`-formatting). Tests live in `tests/test_gateway_app.py` (`test_game_*`): assert 13 screen ids, 8 mode labels, demo banner, and the no-network/no-payment/no-`/api/` invariant. Run with `-k game` (full gateway suite is killed in sandbox).

## /game prototype tests assert on rendered source, not a live DOM
The `/game` demo (`usbay_game_html()`) is one client-side HTML/JS string; its pytest coverage asserts on the text returned by TestClient — there is NO DOM/JS execution.
**Why:** this shapes what assertions are valid. (1) The persistent demo banner uses a plain hyphen ("DEMO ONLY - NO REAL BOOKING") not an em-dash, with "NO REAL PAYMENT" in a separate span. (2) Attributes built by string templating (e.g. pills rendered from a JS key array) never appear as literal attribute values in the source — assert the source-side key array / labels / comparator functions instead of the rendered attribute. (3) To test data-driven behaviour like route precedence, parse the in-page `var TRIPS=[...]` block and compute winners from its numeric fields rather than expecting a computed result string.
**How to apply:** for "no hidden booking/payment" tests, assert PURCHASE call-to-action phrases absent (lowercased "book now"/"buy now"/"pay now"/"checkout"/payment-field names) — never assert bare "book"/"booking", because the banner and demo disclaimers legitimately contain "booking".

## Interactive DOM ("browser-level") tests for /game use jsdom piped through pytest
No Playwright/Selenium/chromium is installable-reliable in this sandbox (npm crashes on the existing pnpm `.pnpm` store — use `pnpm add -D`, not npm). The working pattern for genuine interactive DOM coverage: a Node **jsdom** harness (`tests/game_dom_harness.mjs`, `runScripts:"dangerously"` + `beforeParse` spies) that reads the rendered HTML on stdin, executes the page's real client-side JS, drives the live DOM (`.click()` on toggles/route-finder pills/nav — event delegation bubbles correctly in jsdom), and emits a JSON report; a pytest driver (`tests/test_game_interactive_dom.py`) renders `/game` in-process via `TestClient` (no live server needed — `/game` is static HTML, works without the audit/signing env), runs the harness via `subprocess`, and asserts on the JSON. Skips gracefully if node/jsdom absent.
**Why:** keeps "browser-level" interactive coverage runnable inside the Python pytest suite with no chromium download and no live-server dependency; the `beforeParse` net spies (fetch/XHR/WebSocket/EventSource/sendBeacon) + storage spies (setItem/removeItem/clear) + final `document.cookie` check are the actual *proof* of "no external calls / no persistence" — a real interactive assertion, not a source grep.
**How to apply:** `window.scrollTo` must be stubbed in `beforeParse` (jsdom doesn't implement it; the game calls it on every `show()`); end the harness with `process.stdout.write(json, ()=>{window.close(); process.exit(0)})` so pending jsdom timers don't hang Node. Route-finder winners are deterministic from `RANK`+`TRIPS`: cheapest/fastest="Line 3 - Teal", xp="Pacific Star", gov="Atlas Express R-12". jsdom is a `pnpm` devDependency; root `node_modules` is already git-ignored.

## /game DOM test harness — import-dominated; reuse the session fixture; jsdom is unbundleable
The browser-level `/game` tests run real client JS in jsdom via a session-scoped fixture + Node harness. Wall-clock is dominated by the jsdom MODULE IMPORT (the vast majority of a run); construction/interaction/selectors are minor.
**Rule:** every DOM test file must reuse the existing session-scoped `dom_result` fixture — never give a new file its own module/function fixture, since that adds a whole extra jsdom render. Perf is import COUNT, not per-import time.
**Do NOT try to bundle jsdom to speed up import** — jsdom is effectively unbundleable into one file: esbuild ESM (needed for the harness's top-level await) dies first on dynamic `require` (a `createRequire` banner fixes that), then on `__dirname` because jsdom `readFileSync`s its own data assets (default stylesheet, xhr sync worker) from its package dir at RUNTIME, so a single bundle still needs those files on disk. esbuild was tried and removed.
**Why:** the slow win is structural (1 render/session) + quieting TS-LSP I/O contention via a repo-root `tsconfig.json`. Cold cache can't be force-reproduced here (no root to drop the page cache).

## /game has a registered stability gate — run it after any /game change
`scripts/game_stability_gate.py` (registered validation command **`game-stability`**) is the GAME-010R stability gate: in-process boot + `GET /game`==200, runs the GAME-008/009R/010R DOM suites under ONE shared jsdom render, re-verifies 8 safety properties (demo banner, no booking/payment UI, no network, no persistence, demo-only 20% VIP discount, deterministic routes, child-safe + a11y persist after interactions), enforces a timeout threshold, treats any pytest SKIP as FAIL (no silent skips), and regenerates `evidence/audit/GAME_DEMO_STABILITY_GATE_010R.md`. Run it after touching anything `/game`-related.
**Gotcha:** a standalone `python scripts/x.py` that imports `gateway.app` must `sys.path.insert(0, repo_root)` itself — pytest injects rootdir automatically, plain python does NOT (boot failed with `No module named 'gateway'` until added).
**Single-render hook:** set env `GAME_STABILITY_DUMP=/path.json` and the conftest `dom_result` fixture writes that render's parsed JSON there — read it for benchmark/safety evidence instead of adding a second jsdom render.

## /game demo prototype — screen router + DOM-harness walk-list
The standalone `/game` route (`gateway/app.py::usbay_game_html()`) is a self-contained
client-side prototype built around a `SCREENS` registry (`buildNav`/`show`, `[data-nav]`
routing). Adding a screen = add a render fn + a `SCREENS` entry; nav wiring is automatic.
The seven named deliverable screens are Home Dashboard, World Map, Travel Hub (id `hub`),
Governance Center, Academy, Rewards, Profile.

**Constraint to remember:** `tests/game_dom_harness.mjs` walks a HARD-CODED screen list
(home,map,hub,rail,bus,cruise,ferry,airport,hotel,business,governance,crew,rewards) — it
does NOT include `academy` or `profile`. So new screens are never exercised by the existing
DOM suites; verify them with a targeted jsdom check (assert no `<input>/<select>/<textarea>`,
no booking/payment button text, no FORBID phrases, no net/persist/cookie, no JS errors).
**Why:** keeps the demo-only invariants enforced even on screens CI doesn't traverse.
**How to apply:** when touching `/game`, either add the new screen id to the harness
walk-list or run a separate jsdom pass over it before claiming validation.

Quirk: jsdom is a pnpm install under `node_modules/.pnpm/`; an ad-hoc `.mjs` checker must
run from the workspace root (not `/tmp`) or Node ESM can't resolve `import "jsdom"`.

## GAME-012R — screen visibility & deep-links (durable notes)
- `/game` now has a 16th screen "marketplace" (coming-soon, NOT IMPLEMENTED placeholder — no economy by design) plus per-screen hash deep-links `/game#<id>` (screenFromHash + hashchange + hash-aware init) and ArrowUp/Down keyboard nav on `#nav`; selector buttons carry `type="button"` + `aria-current`.
- **Non-obvious:** the game DOM tests' `== 15` assertions count `tripCount`/`multiModalTripCount` (trips in the hub), NOT screens — adding screens is safe; do NOT "fix" those to 16.
- **Game copy safety:** the screen-corpus safety scan forbids the literal substring "checkout" (and other CTA words) ANYWHERE, even inside negative disclaimers ("no ... checkout") — keep such trigger words out of /game text.
- New screen-visibility harness/test pair: `tests/game_screen_visibility_harness.mjs` + `tests/test_game_screen_visibility_dom.py` (run node/jsdom from workspace ROOT). PNG-per-screen automation NOT available (screenshot tool is JPEG app-preview only, can't batch client hash routes) — deep-links + manual path documented in evidence/audit/USBAY_GAME_012R_SCREEN_VISIBILITY_REPORT.md.

## /game per-screen screenshots: external_url drops `#hash`, use `?s=<id>` query deep-link
The `screenshot` tool's `external_url` renderer strips the URL fragment, so `/game#rail` etc. all boot to the SPA's DEFAULT landing (World Map) — every hash shot came back identical. There is no registered artifact, so `app_preview` is unavailable too.
**Fix (durable):** the game now supports a QUERY-param deep-link in addition to hash — `screenFromQuery()` reads `?s=<screenId>` or `?screen=<screenId>` (validated against `SCREENS`) and boot is `show(screenFromHash()||screenFromQuery()||"map")`. Query params survive the screenshot renderer, so capture per screen via `…/game?s=home|map|hub|rail|bus|cruise|hotel|marketplace`.
**Why:** additive, demo-safe, doesn't change default landing (only used when `?s=` present) and keeps the existing hash deep-link/tests green.

## /game travel-first shell (GAME-016R) — tagline, derived Rep, gameplay-loop buttons
The travel-first landing is already the default: `var active="map"` (World Map). Header tagline "Travel • Earn • Govern • Play" lives in the `.bname <small>`. The wallet "Rep" chip is DERIVED/display-only (`repVal()` = round of avg of `SCORES.fairness/privacy/sustainability`) — it is NOT a stored `W` currency, so it changes only when governance missions move scores (`refreshScores()` now also calls `renderWallet()`). The World-Map "Gameplay loop" card's 6 buttons (Choose Route / Start Demo Trip / Complete Mission / Claim Rewards / Upgrade Crew / Open Marketplace) use `data-loop="route|trip|mission|rewards|crew|market"` + `doLoop()` which dispatch to EXISTING screens/actions (`show(...)`, `doTrip(TRIPS[3])`).
**Why:** keeps the change additive UI-only — no new screens, no new mechanics, no governance/`/execute` touch.
**How to apply:** because the loop buttons reuse existing screen ids, the hard-coded `game_dom_harness.mjs` walk-list does NOT need updating. Adding a genuinely new loop target screen WOULD require updating the harness list (see the harness-walk-list note above).

## /game premium UX (GAME-017R) — crew identity rules, forbidden phrases, hero h1
Premium upgrade is additive UI-only: hero landing block prepended to `scMap` (gradient wordmark + Travel/Earn/Govern/Play tag chips + 5 `.herocard` quick-links via existing `data-nav`/`data-loop`), crew role badges, rewards economy (Reputation card `rwRep`=`repVal()`, top-row Audit/VIP mirrors `rwAuditTop`/`rwVipTop`), `.sim-line` simulated/non-redeemable marker.
**Hard constraints discovered (each caused a real test failure):**
- Hero title MUST NOT be an `<h1>` — `test_world_map_is_default_landing` reads `T($("#main h1"))` and asserts "map" in it. Use `<div class="hero-title">` so the first `#main h1` stays "World Map".
- "No sensitive identity labels" = remove the crew **heritage/ethnicity** field (`her`) from data AND display. But pronouns (`pr`, incl. `they/them`) and regions are REQUIRED: `test_gateway_app.py` asserts `pr:"they/them"` + `len(regions)>=8`; `game_dom_harness.mjs` asserts crew DOM `includes("they/them")`. Keep pronouns+regions, drop heritage only.
- The forbidden-phrase scanner (4 `game_*_harness.mjs` FORBID lists) matches the substring **"real money"** even inside a negation ("no real money"). Never put "real money" in /game copy — use "no monetary value".
**Why:** these are non-obvious cross-file test contracts; violating any one fails the stability gate / DOM suites even though the change "looks" safe.
**game-stability gate note:** the gate's `[forbidden] FAIL - N files changed in working tree` is a working-tree guard, NOT a code failure — it clears on commit (platform auto-commits at task end). All 8 safety props + 23 DOM tests passing = functionally green.

## Root page (governance_gateway_html) — durable gotchas
**Top-of-page placement:** client JS prepends the large governance feature sections to the top of `<main>` at runtime, so a static first-child of `<main>` is pushed far below the fold. To pin content to the very top, place it as a body child BETWEEN `</header>` and `<main>`, and give it `max-width:1340px;margin:auto;padding:0 22px` to match the `main` container.
**Why:** the root is rendered via `_inject_platform_sync_bar`, which inserts the sync bar immediately before the literal two-space `  <main>` marker — preserve that exact marker or the injection silently no-ops.
**Template quirk:** the root uses `string.Template.substitute()` (NOT safe_substitute) — any literal `$` in added HTML/CSS raises at render time; use HTML entities, avoid `$`; CSS `{}` braces are fine.
**Product decision:** the root intentionally EXPOSES the game (product selector + nav link). Any older test/assertion expecting the game to be hidden from root is obsolete — assert governance markers persist instead of asserting the game is absent.

## Regulator-package evidence chain (layered, additive)
The regulator-package source-derivation evidence is a stack of additive, evidence-only, read-only layers (self-derivation → source-gap-closure), each adding stricter provenance/fail-closed checks on top of the last. Rules that hold across the stack: each layer RE-DERIVES hashes from prior evidence but must never mutate or weaken the lower layers; none are wired into `/execute`; reports store only resulting state vocab (never free-text justification, never raw sensitive data); a caller-supplied hash is acceptable ONLY as a DOCUMENTED fallback when the source is genuinely unavailable, otherwise fail-closed.
**Why:** these layers exist to close the "a value may still be caller-supplied" gap for auditors; mixing enforcement into `/execute` or storing justification text would re-open the very gap they close.

## Post-merge / resync: read tool can serve a stale file snapshot
Right after a task merge/reconciliation rewrites a large file, the `read` tool may return a STALE shorter snapshot (wrong total line count, "exceeds file length") while `bash`/`sed`/`rg`/`wc -l` see the true current file. **How to apply:** if `read` and shell disagree on a file's size/contents, trust the shell, re-confirm with `wc -l` + `py_compile`, and use `sed -n`/`rg` to read until `read` catches up.
