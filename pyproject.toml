[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "usbay-policy-brain"
version = "0.0.0"
requires-python = ">=3.9,<3.12"
[tool.setuptools.packages.find]
where = ["."]
include = ["audit*", "memory*", "runtime*"]
exclude = ["tests*"]
